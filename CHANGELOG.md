# Changelog

Entries below are appended automatically by the `changelog.yml` workflow whenever a merge to `main` adds new rows to the `README.md` catalogue table. Each entry is timestamped (UTC) and lists the added rows verbatim.

<!-- changelog-entries-start -->
<!-- changelog-entries-end -->
